# src/

Aquí va el **código de tu aplicación**. En el template está vacío a propósito.

- El `implementer` es el único agente que escribe aquí (ver
  `.claude/agents/implementer.md`).
- El `leader`, el `spec_author` y el `reviewer` **no** tocan esta carpeta.
- La estructura de capas y módulos la defines tú en
  [`docs/architecture.md`](../docs/architecture.md).

Borra este `README.md` cuando añadas tu primer módulo (o déjalo si te sirve de
recordatorio).
